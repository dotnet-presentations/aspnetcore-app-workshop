@page "{id}"
@model DetailsModel

<h1>@Model.Session.Title</h1>
<p>@Model.Session.Track?.Name<p>

<ul>
@foreach (var speaker in Model.Session.Speakers)
{
    <li><a asp-page="Speaker" asp-route-id="@speaker.ID">@speaker.Name</a></li>
}
</ul>