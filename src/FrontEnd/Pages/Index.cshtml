@page
@model IndexModel
@{
    ViewData["Title"] = "Home Page";
}

<h1>NDC Oslo 2017</h1>

@foreach (var day in Model.Sessions)
{
    <h3 id="@day.Key.ToString().ToLowerInvariant()">@(day.Key?.ToString() ?? "No Day")</h3>
    @foreach (var timeSlot in day)
    {
        <h4>@timeSlot.Key?.ToString("HH:mm")</h4>
        <div class="row">
            @foreach (var session in timeSlot)
            {
                <div class="col-md-3">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            <p>@session.Track?.Name</p>
                            <h3 class="panel-title"><a asp-page="Session" asp-route-id="@session.ID">@session.Title</a></h3>
                            @session.Abstract
                            <ul>
                                @foreach(var speaker in session.Speakers)
                                {
                                    <li><a asp-page="Speaker" asp-route-id="@speaker.ID">@speaker.Name</a></li>
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
}
